# MindSight

Project workspace for Mental Health Data Analysis.

## Setup
Run `scripts/setup_env.bat` (Windows) or install requirements via `pip install -r requirements.txt`.

## Data
Data is located in `data/Track_D_Mental_Health/`.
